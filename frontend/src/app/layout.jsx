import { Metadata } from "next";
import "./globals.css";
import VisualEditsMessenger from "../visual-edits/VisualEditsMessenger";
import ErrorReporter from "@/components/ErrorReporter";
import Script from "next/script";

export const metadata: Metadata = {
  title: "SahiPay - Bol ke Banking | Voice-First Digital Banking for Everyone",
  description: "Experience voice-first banking in your language. Build trust, prevent scams, and bank with confidence. Accessible, simple, secure.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children;
}>) {
  return (
    <html lang="en">
      <body className="antialiased bg-variant-medium">
        <ErrorReporter />
        <Script
          src="https://slelguoygbfzlpylpxfs.supabase.co/storage/v1/object/public/scripts//route-messenger.js"
          strategy="afterInteractive"
          data-target-origin="*"
          data-message-type="ROUTE_CHANGE"
          data-include-search-params="true"
          data-only-in-iframe="true"
          data-debug="true"
          data-custom-data='{"appName": "YourApp", "version": "1.0.0", "greeting": "hi"}'
        />
        {children}
        <VisualEditsMessenger />
      </body>
    </html>
  );
}